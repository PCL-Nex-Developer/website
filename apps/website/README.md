# PCL Nex Website

The PCL Nex website, based on the official Axolotl Launcher website source.

## Development

From the repository root:

```sh
pnpm install
pnpm dev
```

Create the static production output with `pnpm build`.

The imported upstream source and license details are recorded in
[`../../UPSTREAM.md`](../../UPSTREAM.md).
